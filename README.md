Zemberek Solr Filter
============

This filter replaces the token with its stem if it can find any Turkish stem using the Zemberek NLP library

You have to install Zemberek dependencies into your local maven repository for building this project. Or you can add a Maven repository that contains Zemberek jars.

